#include "main.h"
/**
 *_puts - swap 2 ints
 * @str: string
* Return: Returns the string length
 */



void _puts(char *str)
{
int i;
for (i = 0 ; *str = '\0' ; i++)
{
_putchar(str[i] + 0);
}
_putchar('\n');
}
